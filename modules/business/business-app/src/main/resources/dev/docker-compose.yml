version: '3'
services:
  mysql:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: reservapppass
      MYSQL_DATABASE: reservapp
    volumes:
      - db:/var/lib/mysql
    ports:
      - "3306:3306"

volumes:
  db: